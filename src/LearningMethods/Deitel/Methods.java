/*
package Deitel;

public class Methods {
    public static void main(String[] args) {
        Methods methods = new Methods();
        methods.divideNumber(2, 5);
        methods.mathPow(5, 3);
        methods.mathPow(2, 4);
        methods.multiplication(5, 4, 7);
        methods.multiplication(-4, 5);
       */
/* int numToBeUsed = methods.addNumbers(7, 2);
        methods.addNumbers(numToBeUsed, 10);
        int collectedNum = methods.divisionNumbers(2,10);
        methods.divisionNumbers(collectedNum,2);*//*

    }

    public int addNumbers(int num1, int num2) {
        int result = num1 + num2;
        System.out.println(result);
        return result;
    }

    public double mathPow(double num1, double num2) {
        double returnResult = 1;
        for (int count = 0; count < num2; count++) {
            returnResult = returnResult * num1;
        }
        System.out.println(returnResult);
        return returnResult;
    }

    public double mathFlow(double floorNumberValue, double number) {
        boolean preCondition = floorNumberValue <= number && number <= floorNumberValue + 1;
       double sum = (-floorNumberValue <-1) <= - number


        }
        return resultMathFlow;
    }


    public int divisionNumbers(int num1, int num2) {
        int returnResult = num1 / num2;
        return returnResult;
    }

    public double divideNumber(double num1, double num2) {
        double result;
        if (num1 > num2) {
            result = num1 / num2;
        } else {
            result = num2 / num1;
        }
        System.out.println(result);
        return result;
    }

    public double multiplication(double num1, double num2) {
        double multiplyResult = num1 * num2;
        System.out.println(multiplyResult);
        return multiplyResult;
    }

    public double multiplication(double num1, double num2, double num3) {
        double multiplyResult = num1 * num2 * num3;
        System.out.println(multiplyResult);
        return multiplyResult;

    }
}
*/
